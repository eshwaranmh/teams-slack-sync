{
  "name": "teams-slack-sync-server",
  "version": "2.0.0",
  "private": true,
  "description": "Local server that syncs Slack status from Teams (web and desktop app) meeting state",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=22"
  }
}
